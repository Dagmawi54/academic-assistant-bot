services:
  - type: web
    name: academic-assistant-bot
    env: docker
    dockerfilePath: Dockerfile
    plan: free
    healthCheckPath: /health
    envVars:
      - key: DATABASE_URL
        sync: false # Set this to your Neon connection string in the Render UI
      - key: USE_POLLING
        value: "True" # Switch to False once WEBHOOK_URL is set
      # These must be set securely in the Render Dashboard
      - key: BOT_TOKEN
        sync: false
      - key: GROQ_API_KEY
        sync: false
      - key: GEMINI_API_KEY
        sync: false
      - key: RENDER_EXTERNAL_URL
        fromService:
          type: web
          name: academic-assistant-bot
          property: url
      - key: REDIS_URL
        fromService:
          type: redis
          name: bot-redis
          property: connectionString

  - type: redis
    name: bot-redis
    ipAllowList: [] # only allow internal connections
    plan: free
